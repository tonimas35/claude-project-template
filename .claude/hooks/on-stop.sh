#!/bin/bash
# Runs when Claude finishes responding.
# Good place for: lint checks, test runs, build validation.
# Example:
#   npm run lint --silent 2>&1 | head -20
#   npm test --silent 2>&1 | tail -10

exit 0
